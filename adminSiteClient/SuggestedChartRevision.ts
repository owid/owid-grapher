import { GrapherInterface } from "../grapher/core/GrapherInterface"

export enum SuggestedChartRevisionStatus {
    pending = "pending",
    approved = "approved",
    rejected = "rejected",
    flagged = "flagged",
}

