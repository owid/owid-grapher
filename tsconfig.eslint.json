{
    "extends": "./tsconfig.json",
    "include": [
        ".eslintrc.cjs",
        "**/*.ts",
        "**/*.tsx",
        "**/*.mts",
        "**/*.mtsx",
        "**/*.cts",
        "**/*.ctsx"
    ],
    "compilerOptions": {
        // ensure that nobody can accidentally use this config for a build
        "noEmit": true
    }
}
