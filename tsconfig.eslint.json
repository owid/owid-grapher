{
    "extends": "./tsconfig.json",
    "include": [
        "**/*.ts",
        "**/*.tsx",
        "**/*.mts",
        "**/*.mtsx",
        "**/*.cts",
        "**/*.ctsx"
    ],
    "compilerOptions": {
        // ensure that nobody can accidentally use this config for a build
        "noEmit": true
    }
}
