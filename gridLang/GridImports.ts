// todo: should we just duplicate these functions and then not depend on these other subprojects?

import { imemo, trimMatrix, trimArray } from "coreTable/CoreTableUtils"
import { GitCommit } from "gitCms/GitTypes"
import { isPresent } from "clientUtils/isPresent"

export { imemo, trimMatrix, GitCommit, isPresent, trimArray }
