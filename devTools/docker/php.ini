xdebug.mode = debug
xdebug.client_host = host.docker.internal

; "trigger" is the default in debug mode. In the browser devtools, create a
; cookie named XDEBUG_TRIGGER to start step-by-step debugging.
; Change to "yes" for starting unconditionally
; xdebug.start_with_request = trigger

; xdebug.log = /tmp/xdebug.log
