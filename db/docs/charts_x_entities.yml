metadata:
    description: Many-to-many table linking charts to entities to track which geographic or organizational entities are featured in each chart.
fields:
    chartId:
        description: Foreign key to charts table
    entityId:
        description: Foreign key to entities table
