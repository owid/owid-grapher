# CLA signatures storage

This branch stores CLA Assistant signatures as JSON. Do not protect it
(the CLA Assistant action needs to push to it). See
../master/.github/workflows/cla.yml and ../master/CONTRIBUTOR_AGREEMENT.md.

