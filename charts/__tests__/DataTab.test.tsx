#! /usr/bin/env jest
import fs = require("fs")
import path = require("path")
import { encode } from "entities"

describe("DataTab", () => {
    test("shahid", () => {
        // nothing yet
    })
})
